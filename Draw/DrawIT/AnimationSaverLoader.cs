using System;
using System.Text.Json;
using System.IO;

namespace Draw
{
    static class AnimationSaverLoader
    {
        public static void SaveAnimation(Animation anim)
        {
            //TODO: FIX

            //Console.WriteLine("Enter out folder path:");
            //string path = Console.ReadLine();
            //string animName = "anim";
            //if(!File.Exists(path+animName+".txt"))
            //{
            //    //File.Create(path + animName + ".txt");
            //}
            //string output = "";
            //for (int i = 0; i < anim.Count; i++)
            //{
            //    output += $"f{i}:{{{anim.canvas.GetProps()}";
            //}

        }
    }
}
